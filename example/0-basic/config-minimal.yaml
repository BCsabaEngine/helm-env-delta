# Minimal configuration for testing the suggest command
# This config lacks transforms and stop rules, which the suggestion engine should detect

source: './source'
destination: './destination'

# Sync all YAML files
include:
  - '**/*.yaml'

# Skip environment-specific paths
skipPath:
  '**/*.yaml':
    - 'environment'
    - 'cluster.name'
